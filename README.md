# Second Try
Trying to remake the website with the knowledge I've learned over the months.

## TODO

- Use .scss instead of .module.scss
- Name classes with camel case instead of dashes
- Clean components (no less than 10 lines, no more than 150 lines)
- Use cleaner architectures for directories
- routes + screens
- distinguish better between local and api movies
- Clean types
